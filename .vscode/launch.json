{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug C++ with LLDB",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cmake: build"
    }
  ]
}
